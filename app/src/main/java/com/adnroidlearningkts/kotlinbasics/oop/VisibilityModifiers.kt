package com.adnroidlearningkts.kotlinbasics.oop

/**
 * public, private, protected, internal
 *
 * public(default); visible everywhere, both inside and outside the current model
 * private: elements only visible within the file or class where they are declared
 * protected: elements are visible within the file or class where they are declared and its subclass
 * internal: elements are visible within the same module
 *  - a module is a set of Kotlin files that are compiled together(same package)
 */


class VisibilityModifiers {
}